# PixelDeck Widgets scripts

The PowerShell action runs scripts from this directory without opening a console window.
Use the script file name without `.ps1` in the Property Inspector. Names are restricted to
letters, numbers, dots, dashes, and underscores, and the file must exist in this directory.
